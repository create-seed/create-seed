---
"create-seed": minor
---

Add template tool requirements via `create-seed.tools`, including version checks during scaffolding and a `--allow-missing-tools` escape hatch for environments where required commands are intentionally absent.
