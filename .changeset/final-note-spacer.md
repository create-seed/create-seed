---
"create-seed": patch
---

Support `~` as a blank-line marker in template-defined final instructions, so generated next steps can separate related groups of commands without changing the `instructions` schema.
