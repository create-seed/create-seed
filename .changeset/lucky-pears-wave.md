---
"create-seed": minor
---

Add a `template validate` command for checking local `create-seed` metadata, persist normalized template `tools` in generated registries, and surface clearer Zod errors for invalid tool definitions.
